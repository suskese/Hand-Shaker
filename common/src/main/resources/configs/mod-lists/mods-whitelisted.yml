# Whitelisted mods which are allowed but not required
# Only used when whitelist mode is enabled in config.yml
# Format: mod_id:version|null: action (action is from mods-actions.yml or default 'none')

whitelisted:
  sodium:null: none

# Optional mods which are allowed but not required
optional:
  jade:null: none
